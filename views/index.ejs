<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Native Hawaiian Flowers Field Guide</title>
    <link rel="stylesheet" href="/public/style.css">
</head>
<body>
    <h1>Native Flowers!</h1>

    <ul class="plants">
        <% for(let i=0; i < info.length; i++) {%>
            <li class="plant">
                <span><%= info[i].name %></span>
                <span><%= info[i].common_name %></span>
                <span><%= info[i].scientific_name %></span>
                <span><%= info[i].family_name %></span>
                <span><%= info[i].use %></span>
            </li>
        <% } %>
        </ul>

    <script src="/public/main.js"></script>
</body>
</html>